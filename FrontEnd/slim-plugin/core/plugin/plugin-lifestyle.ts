import { Plugin } from "./plugin-type";

class PluginLifestyle {
  constructor() {}
  mountPlugin(plugin: Plugin): boolean {
    let res: boolean = false;

    return res;
  }
  unmountPlugin(plugin: Plugin): boolean {
    let res: boolean = false;
    return res;
  }

  enablePlugin(pluginId: string): boolean {
    let res: boolean = false;
    return res;
  }

  disablePlugin(pluginId: string): boolean {
    let res: boolean = false;
    return res;
  }
}
